{
  "name": "balkhaev.github.io",
  "version": "0.1.0",
  "description": "Balkhaev homepage",
  "main": "src/main.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "parcel build src/index.html && git push origin :gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [
    "homepage"
  ],
  "author": "Balkhaev",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.11.0"
  }
}
